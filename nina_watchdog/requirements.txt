# NINA Safety Monitor Requirements
# Install with: pip install -r requirements.txt

# Core dependencies with exact versions (freeze output)
astropy==7.1.1
astropy-iers-data==0.2025.11.3.0.38.37
attrs==25.4.0
jsonschema==4.25.1
jsonschema-specifications==2025.9.1
numpy==2.3.4
packaging==25.0
paho-mqtt==2.1.0
pyerfa==2.0.1.5
pywin32==311
PyYAML==6.0.3
referencing==0.37.0
rpds-py==0.28.0

# GUI dependencies for system tray application
pystray==0.19.5
Pillow==11.0.0

# Minimum version requirements (for reference)
# paho-mqtt>=1.6.1     # MQTT communication for obsybox integration
# astropy>=5.0         # Astronomical calculations for sun altitude checking
# pywin32>=306         # Windows COM interface for ASCOM telescope/dome control
# jsonschema>=4.0      # JSON schema validation (optional)

# Push notifications
requests>=2.31.0      # HTTP requests for Pushover API

# Email notifications (optional)
# Uncomment if using email alerts
# smtplib is built-in to Python
# email-validator>=1.3.0